#ifndef __OBJ_WEB_H__
#define __OBJ_WEB_H__

/*
    文档简介：WEB展示区--窗体控件
*/

#include <QWebFrame>
#include <QWebView>
#include <QString>
#include <QWidget>

#include <iostream>
#include <cstdlib>
#include <cstdio>
#include <string>
#include <cstring>
#include <unistd.h>

#include "conf.h"
#include "cpp_datetimer.h"
#include "../include/json/json.h"

class obj_web: public QWebView{
    Q_OBJECT

    private:
        QString url;                            //网络地址
        QWebFrame *webFrame;                    //WEB控件
        Json::FastWriter JWriter;               //JSON去格式话字符串生成器
        std::string response;                   //web返回数据
        std::string liftInfo_bak;               //设备列表信息-备份
        std::string liftInfo_rel;               //设备列表信息-实时

    public:
        obj_web(const char *_url=WEB_DEFAULT_URL, QWidget *_parent = 0);
        ~obj_web();

    public:
        int init();                                                                     //初始化
        int reload_url(const char *_url);                                               //重新加载指定url
        int reload_url();

    public slots:
        void web_window_connect();
        QString get_realData(); 
        QString get_liftReal();
        QString checkUpdata_liftInfo();

};

#endif